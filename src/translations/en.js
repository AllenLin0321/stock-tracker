import record from 'translations/record/en';
import navBar from 'translations/navBar/en';
import searchBar from 'translations/searchBar/en';

export default {
  ...record,
  ...navBar,
  ...searchBar,
};
